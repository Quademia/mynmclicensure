# CLAUDE.md — MyNMCLicensure

The rules for this repo live in **[`AGENTS.md`](AGENTS.md)**. Read it
first and follow it; it is the one rules file, shared with Codex.

Claude-specific: this file is auto-loaded by Claude Code. Nothing else
goes here — a rule belongs in `AGENTS.md`, history in `sessions/`.
