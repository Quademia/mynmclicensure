// app/page.tsx — placeholder.
//
// Proves the stack serves. Replaced by the transcribed public index page
// (legacy/mynmclicensure/index.html) in rebuild slice 3.
export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "3rem" }}>
      <h1>MyNMCLicensure</h1>
      <p>Rebuild scaffold — slice 1. Nothing to see here yet.</p>
    </main>
  );
}
